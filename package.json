{
  "name": "simple-token-bridge",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/unionmini/simple-token-bridge.git",
  "author": "unionmini <unioonmini@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "solhint": "^5.1.0"
  },
  "scripts": {
    "lint": "solhint 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol'",
    "lint:fix": "solhint 'src/**/*.sol' 'script/**/*.sol' 'test/**/*.sol' --fix"
  }
}
